---
$schema: https://raw.githubusercontent.com/JanDeDobbeleer/oh-my-posh/main/themes/schema.json
blocks:
  # - alignment: left
  #   segments: 
  #     - background: '#FEF5ED'
  #       foreground: '#011627'
  #       leading_diamond: 
  #       style: diamond
  #       template:  {{ .Name }}
  #       trailing_diamond: <transparent,#FEF5ED></>
  #       type: shell
  #     - background: '#575656'
  #       foreground: '#d6deeb'
  #       leading_diamond: 
  #       properties:
  #         style: roundrock
  #         threshold: 0
  #       style: diamond
  #       template: ' {{ .FormattedMs }} '
  #       trailing_diamond: 
  #       type: executiontime
  #   type: prompt
  - alignment: left
    # newline: true
    segments:
      - style: plain
        template: ╭─
        type: text
      - properties:
          time_format: 15:04
        style: plain
        template: ' <#FAD6A5></> | {{ .CurrentDate | date .Format }} |'
        type: time
      - style: plain
        template: '  '
        type: root
      - properties:
          folder_icon: ' '
          folder_separator_icon: '  '
          home_icon: ' '
        style: plain
        template: ' {{ .Path }} '
        type: path
    type: prompt
  - alignment: right
    newline:
    segments:
      - background: '#F0F0F0'
        foreground: '#011627'
        leading_diamond: 
        properties:
          branch_icon: ' '
          fetch_stash_count: true
          fetch_status: true
          fetch_upstream_icon: true
          fetch_worktree_count: true
        style: diamond
        template: ' {{ .HEAD }}{{if .BranchStatus }}{{ end }}{{ if .Working.Changed
          }}  {{ .Working.String }}{{ end }}{{ if and (.Working.Changed) (.Staging.Changed)
          }} |{{ end }}{{ if .Staging.Changed }}  {{ .Staging.String }}{{ end }}{{
          if gt .StashCount 0 }}  {{ .StashCount }}{{ end }} '
        trailing_diamond: 
        type: git
    type: prompt
  - alignment: left
    newline: true
    segments:
      - properties:
          always_enabled: true
        style: plain
        template: '╰─ $ '
        type: status
    type: prompt
console_title_template: '{{ .Folder }}'
transient_prompt:
  background: transparent
  foreground: '#FEF5ED'
  template: ' '
version: 3
final_space: false
